<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Para ti preciosa</title>
  <style>
    *,
    *::after,
    *::before {
      padding: 0;
      margin: 0;
      box-sizing: border-box;
    }

    :root {
      --dark-color: #000;
      --fl-speed: 0.8s;
      --speed-leaf: 2s;
    }

    body {
      display: flex;
      align-items: flex-end;
      justify-content: center;
      min-height: 100vh;
      background-color: var(--dark-color);
      overflow: hidden;
      perspective: 1000px;
    }

    /* ===== FRASE ANIMADA ===== */
    .mensaje {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      z-index: 200;
      text-align: center;
      animation: fadeOut 5s ease forwards;
      animation-delay: 3s;
    }

    .mensaje h1 {
      font-size: 3rem;
      font-weight: bold;
      color: #fff;
      letter-spacing: 5px;
      text-transform: uppercase;
      animation: subir 2s ease-out forwards;
    }

    @keyframes subir {
      0% {
        opacity: 0;
        transform: translate(-50%, 50px);
      }
      100% {
        opacity: 1;
        transform: translate(-50%, -50%);
      }
    }

    @keyframes fadeOut {
      to {
        opacity: 0;
        visibility: hidden;
      }
    }

    /* ===== ESTRELLAS FUGACES ===== */
    .shooting-stars {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      overflow: hidden;
      z-index: 10;
    }

    .shooting-star {
      position: absolute;
      top: 0;
      left: 50%;
      width: 3px;
      height: 80px;
      background: linear-gradient(180deg, #fff, transparent);
      animation: shooting 3s linear infinite;
      opacity: 0.8;
    }

    .shooting-star:nth-child(2) { left: 20%; animation-delay: 1s; }
    .shooting-star:nth-child(3) { left: 70%; animation-delay: 2s; }
    .shooting-star:nth-child(4) { left: 40%; animation-delay: 1.5s; }
    .shooting-star:nth-child(5) { left: 80%; animation-delay: 2.5s; }
    .shooting-star:nth-child(6) { left: 30%; animation-delay: 0.5s; }
    .shooting-star:nth-child(7) { left: 60%; animation-delay: 2.2s; }
    .shooting-star:nth-child(8) { left: 10%; animation-delay: 1.2s; }

    @keyframes shooting {
      0% { transform: translateY(-100px) translateX(0); opacity: 0; }
      10% { opacity: 1; }
      100% { transform: translateY(100vh) translateX(200px); opacity: 0; }
    }

    /* ===== FLORES (resumen de tu estilo original) ===== */
    .flowers {
      position: relative;
      transform: scale(0.9);
    }

    .flower {
      position: absolute;
      bottom: 10vmin;
      transform-origin: bottom center;
      z-index: 20;
    }

    /* puedes pegar aquí todo tu CSS de flores tal cual lo tenías */
  </style>
</head>
<body>
  <!-- Frase animada -->
  <div class="mensaje">
    <h1>PARA TI PRECIOSA</h1>
  </div>

  <!-- Estrellas fugaces -->
  <div class="shooting-stars">
    <div class="shooting-star"></div>
    <div class="shooting-star"></div>
    <div class="shooting-star"></div>
    <div class="shooting-star"></div>
    <div class="shooting-star"></div>
    <div class="shooting-star"></div>
    <div class="shooting-star"></div>
    <div class="shooting-star"></div>
  </div>

  <!-- Flores -->
  <div class="flowers">
    <!-- Aquí va tu estructura completa de girasoles -->
    <!-- Ejemplo de una flor: -->
    <div class="flower flower--1">
      <div class="flower__leafs flower__leafs--1">
        <div class="flower__leaf flower__leaf--1"></div>
        <div class="flower__leaf flower__leaf--2"></div>
        <div class="flower__leaf flower__leaf--3"></div>
        <div class="flower__leaf flower__leaf--4"></div>
        <div class="flower__white-circle"></div>
      </div>
      <div class="flower__line">
        <div class="flower__line__leaf flower__line__leaf--1"></div>
        <div class="flower__line__leaf flower__line__leaf--2"></div>
        <div class="flower__line__leaf flower__line__leaf--3"></div>
        <div class="flower__line__leaf flower__line__leaf--4"></div>
      </div>
    </div>
    <!-- duplica y ajusta para más flores -->
  </div>
</body>
</html>
